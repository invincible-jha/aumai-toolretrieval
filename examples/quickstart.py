"""Minimal working example for aumai-toolretrieval.

Run this file directly to verify your installation:

    python examples/quickstart.py
"""

# Toolretrieval — Vector-indexed tool search for large tool registries
# Replace the placeholder below with your first real usage example.


def main() -> None:
    """Entry point for the quickstart example."""
    print("aumai-toolretrieval quickstart — replace this with real usage.")


if __name__ == "__main__":
    main()
